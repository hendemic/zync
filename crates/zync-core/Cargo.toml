[package]
name = "zync-core"
version.workspace = true
edition.workspace = true
license.workspace = true

# Deliberately free of platform and I/O dependencies: `cargo test -p zync-core`
# must run on any operating system, including ones with no capture backend yet.
[dependencies]
anyhow.workspace = true
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
